name: animation_playground_app
description: "A Flutter project thanks to JideGuru."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1
  
dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_shaders: ^0.1.3
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^4.0.0
  
flutter:
  uses-material-design: true

  assets:
    - assets/images/

  shaders:
    - shaders/metaball_shader.frag
    - shaders/pyramid.glsl